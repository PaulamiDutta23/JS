if (isPremiumUser) {
  discount = basePrice * 0.2;
  finalPrice = basePrice - discount;
} else {
  discount = basePrice * 0.05;
  finalPrice = basePrice - discount;
}

discount = basePrice * 0.05;
finalPrice = basePrice - discount;

if (isPremiumUser) {
  discount = basePrice * 0.2;
  finalPrice = basePrice - discount;
}